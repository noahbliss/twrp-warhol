# warhol device sepolicy file_contexts
# The recovery runs permissive (see BoardConfig.mk), so this is mostly a
# placeholder for when/if the policy is tightened.
/thp(/.*)?            u:object_r:rootfs:s0
/vendor/firmware(/.*)? u:object_r:vendor_file:s0
